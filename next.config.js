/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: ["img.clerk.com"],
	},
	// Additional configuration options can be added here
};

module.exports = {
	...nextConfig,
};
